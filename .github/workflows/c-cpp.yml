name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: cd Y22 && ./configure.sh
    - name: make
      run: cd Y22 && make
    - name: make check
      run: cd Y22 && make check
    - name: make distcheck
      run: cd Y22 && make distcheck
